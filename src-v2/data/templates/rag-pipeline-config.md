---
name: rag-pipeline-config
description: RAG pipeline configuration with multi-tenant vector storage
category: rag
version: 1.0.0
type: template
web_research_enabled: true
source_verification: true
related_patterns: [rag-pipeline, vector-store-multi-tenant, semantic-chunking, embedding-lifecycle]
---

## Purpose

Document RAG (Retrieval-Augmented Generation) pipeline configuration with tenant-isolated vector storage

# RAG Pipeline Configuration - {{project_name}}

> Generated by `bmad-bam-rag-pipeline` workflow.

**Project:** {{project_name}}
**Date:** {{date}}
**Version:** 1.0.0
**Author:** {{author}}

---

## RAG Architecture Overview

<!-- FILL: Document RAG pipeline architecture -->
```
┌─────────────────────────────────────────────────────────────────┐
│                    RAG Pipeline (Per-Tenant)                     │
│                                                                  │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐      │
│  │  Ingest │───▶│  Chunk  │───▶│  Embed  │───▶│  Store  │      │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘      │
│       │                                             │           │
│       │         ┌─────────────────────────┐        │           │
│       │         │    Query Pipeline       │        │           │
│       │         │  ┌───────┐  ┌───────┐  │        │           │
│       └────────▶│  │Retrieve│──│Generate│ │◀───────┘           │
│                 │  └───────┘  └───────┘  │                     │
│                 └─────────────────────────┘                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## Vector Store Configuration

<!-- FILL: Document vector store settings -->
| Setting | Value | Description |
|---------|-------|-------------|
| Provider | [Pinecone/Weaviate/Qdrant/pgvector] | Vector database |
| Dimensions | 1536 | Embedding dimensions |
| Similarity Metric | cosine | Distance metric |
| Index Type | HNSW | Approximate NN algorithm |

### Tenant Isolation Model

| Model | Implementation | Use Case |
|-------|----------------|----------|
| Namespace | tenant_id namespace | Default |
| Collection | tenant_{id} collection | High isolation |
| Database | Separate instance | Enterprise |

```yaml
vector_store:
  provider: enum[pinecone, weaviate, qdrant, pgvector]
  tenant_isolation: enum[namespace, collection, database]
  
  connection:
    host: string
    api_key: encrypted
    
  index_config:
    dimensions: 1536
    metric: cosine
    replicas: 2
```

---

## Chunking Strategy

<!-- FILL: Document chunking configuration -->
| Strategy | Chunk Size | Overlap | Use Case |
|----------|------------|---------|----------|
| Fixed | 512 tokens | 50 tokens | General text |
| Semantic | Variable | Sentence boundary | Documents |
| Recursive | 1000 chars | 200 chars | Code/structured |
| Custom | {{chunk_size}} | {{overlap}} | Domain-specific |

### Chunking Configuration

```yaml
chunking:
  strategy: enum[fixed, semantic, recursive, custom]
  
  fixed:
    chunk_size: 512
    overlap: 50
    
  semantic:
    model: sentence-transformers
    min_chunk: 100
    max_chunk: 1000
    
  metadata_extraction:
    enabled: true
    fields: [title, source, timestamp]
```

---

## Embedding Configuration

<!-- FILL: Document embedding model settings -->
| Model | Dimensions | Provider | Cost |
|-------|------------|----------|------|
| text-embedding-3-small | 1536 | OpenAI | Low |
| text-embedding-3-large | 3072 | OpenAI | Medium |
| voyage-2 | 1024 | Voyage | Medium |
| Custom | {{dimensions}} | Self-hosted | Variable |

### Embedding Pipeline

```yaml
embedding:
  model: string
  provider: enum[openai, anthropic, voyage, self-hosted]
  batch_size: 100
  rate_limit: 3000/min
  
  fallback:
    enabled: true
    model: text-embedding-3-small
    
  caching:
    enabled: true
    ttl: 7d
```

---

## Retrieval Configuration

<!-- FILL: Document retrieval settings -->
| Parameter | Value | Description |
|-----------|-------|-------------|
| Top K | 5 | Number of results |
| Score Threshold | 0.7 | Minimum similarity |
| Reranking | [true/false] | Enable reranker |
| Hybrid Search | [true/false] | Combine dense + sparse |

### Query Pipeline

```yaml
retrieval:
  top_k: 5
  score_threshold: 0.7
  
  reranking:
    enabled: true
    model: cross-encoder
    top_n: 3
    
  hybrid_search:
    enabled: true
    dense_weight: 0.7
    sparse_weight: 0.3
    
  filters:
    - field: tenant_id
      operator: equals
      value: "{{tenant_id}}"
```

---

## Knowledge Refresh

<!-- FILL: Document refresh strategy -->
| Source Type | Refresh Interval | Method |
|-------------|------------------|--------|
| Documents | On upload | Immediate |
| Web content | Daily | Scheduled |
| Database | Hourly | CDC stream |
| API feeds | 15 minutes | Polling |

### Refresh Configuration

```yaml
knowledge_refresh:
  schedules:
    - source: documents
      trigger: on_change
      
    - source: web_content
      cron: "0 0 * * *"
      
    - source: database
      method: cdc
      
  versioning:
    enabled: true
    retention: 30d
```

---

## Citation Management

<!-- FILL: Document citation tracking -->
| Citation Type | Format | Tracking |
|---------------|--------|----------|
| Document | [Source: filename, page] | Enabled |
| Web | [URL, date accessed] | Enabled |
| Database | [Table, record ID] | Enabled |

---

## Tier-Based Limits

<!-- FILL: Document tier limits -->
| Tier | Vector Storage | Queries/Day | Documents |
|------|----------------|-------------|-----------|
| Free | 10,000 vectors | 100 | 50 |
| Pro | 1M vectors | 10,000 | 5,000 |
| Enterprise | Unlimited | Unlimited | Unlimited |

---

## Monitoring

<!-- FILL: Document RAG metrics -->
| Metric | Type | Alert Threshold |
|--------|------|-----------------|
| retrieval_latency_p99 | Histogram | > 500ms |
| embedding_error_rate | Counter | > 1% |
| relevance_score_avg | Gauge | < 0.6 |
| storage_utilization | Gauge | > 90% |

---

## Web Research Queries

- "RAG pipeline architecture best practices {{date}}"
- "multi-tenant vector database patterns {{date}}"
- "semantic chunking strategies LLM {{date}}"
- "embedding model selection {{date}}"
- "hybrid search RAG implementation {{date}}"

_Source: [URL]_ for implementation guidance.

---

## Verification Checklist

- [ ] Vector store configured and tested
- [ ] Chunking strategy documented
- [ ] Embedding model selected
- [ ] Retrieval pipeline tested
- [ ] Knowledge refresh scheduled
- [ ] Citation tracking enabled
- [ ] Tier limits configured
- [ ] Monitoring active
- [ ] **CRITICAL:** Tenant isolation verified - no cross-tenant retrieval

---

## Change Log

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0.0 | {{date}} | {{author}} | Initial RAG pipeline configuration |
