---
name: tool-contract
description: Document AI agent tool interface contracts
category: ai-runtime
version: 2.0.0
type: template
web_research_enabled: true
source_verification: true
---

## Purpose

Document AI agent tool contracts with tenant context requirements

# Tool Contract - {{tool_name}}

> Generated by `bmad-bam-tool-contract` workflow.

**Tool:** {{tool_name}}
**Version:** {{version}}
**Date:** {{date}}

---

## Tool Overview

<!-- FILL: Document tool purpose -->
| Property | Value |
|----------|-------|
| Tool Name | {{tool_name}} |
| Category | {{category}} |
| Version | {{version}} |
| Status | Active / Deprecated |
| Tenant Aware | Yes / No |

**Description:** [What this tool does and when agents should use it]

---

## Interface Definition

### Input Schema

<!-- FILL: Document input parameters -->
```json
{
  "name": "{{tool_name}}",
  "description": "{{description}}",
  "parameters": {
    "type": "object",
    "properties": {
      "param1": {
        "type": "string",
        "description": "Description of param1"
      },
      "param2": {
        "type": "integer",
        "description": "Description of param2"
      }
    },
    "required": ["param1"]
  }
}
```

### Output Schema

```json
{
  "type": "object",
  "properties": {
    "result": {
      "type": "string",
      "description": "The result of the tool execution"
    },
    "metadata": {
      "type": "object",
      "properties": {
        "execution_time_ms": { "type": "integer" },
        "tenant_id": { "type": "string" }
      }
    }
  }
}
```

---

## Tenant Context

<!-- FILL: Document tenant handling -->
### Context Requirements

| Requirement | Mandatory | Source |
|-------------|-----------|--------|
| tenant_id | Yes | TenantContext |
| user_id | No | UserContext |
| tier | No | TenantContext |

### Context Propagation

```
Agent Request
    │
    ├─► Extract TenantContext
    │
    ▼
Tool Execution
    │
    ├─► Validate tenant access
    ├─► Apply tenant filters
    │
    ▼
Return Result (tenant-scoped)
```

### Data Access

| Access Type | Tenant Filtering |
|-------------|------------------|
| Database queries | RLS policy applied |
| Cache lookups | Tenant-prefixed keys |
| External APIs | Tenant credentials |

---

## Behavior Specification

### Success Case

**Input:**
```json
{
  "param1": "example",
  "param2": 42
}
```

**Output:**
```json
{
  "result": "Processed example successfully",
  "metadata": {
    "execution_time_ms": 150,
    "tenant_id": "tenant_123"
  }
}
```

### Error Cases

<!-- FILL: Document error scenarios -->
| Error | Condition | Response |
|-------|-----------|----------|
| INVALID_INPUT | Missing required param | `{"error": "param1 is required"}` |
| TENANT_NOT_FOUND | Invalid tenant context | `{"error": "Tenant context required"}` |
| ACCESS_DENIED | Unauthorized for tenant | `{"error": "Access denied for tenant"}` |
| RATE_LIMITED | Exceeded tier limit | `{"error": "Rate limit exceeded", "retry_after": 60}` |

---

## Rate Limiting

<!-- FILL: Document rate limits -->
| Tier | Limit | Window | Behavior |
|------|-------|--------|----------|
| FREE | 10/min | 1 minute | Reject |
| PRO | 100/min | 1 minute | Reject |
| ENTERPRISE | 1000/min | 1 minute | Soft limit |

---

## Side Effects

<!-- FILL: Document any side effects -->
| Side Effect | Description | Idempotent |
|-------------|-------------|------------|
| Database write | Updates X table | No |
| Event published | Emits Y event | Yes (deduped) |
| External API call | Calls Z service | Depends |

---

## Dependencies

<!-- FILL: Document tool dependencies -->
| Dependency | Type | Required | Fallback |
|------------|------|----------|----------|
| Database | Infrastructure | Yes | None |
| Cache | Infrastructure | No | Skip cache |
| External API | Service | No | Error response |

---

## Testing

### Unit Test

```javascript
describe('{{tool_name}}', () => {
  it('should process valid input', async () => {
    const result = await tool.execute({
      param1: 'test',
      tenantContext: mockTenantContext
    });
    expect(result.result).toBeDefined();
  });

  it('should reject missing tenant context', async () => {
    await expect(tool.execute({ param1: 'test' }))
      .rejects.toThrow('Tenant context required');
  });
});
```

### Integration Test

```javascript
it('should respect tenant isolation', async () => {
  // Setup: Create data for tenant A
  // Execute: Query with tenant B context
  // Assert: Tenant B sees no data
});
```

---

## Versioning

### Version History

| Version | Date | Changes |
|---------|------|---------|
| 1.0.0 | {{date}} | Initial release |

### Deprecation Policy

| Event | Timeline | Action |
|-------|----------|--------|
| Deprecation announced | -6 months | Warning in response |
| Last supported | Sunset date | Tool removed |

---

## Web Research Queries

- "AI agent tool design patterns {{date}}"
- "function calling best practices {{date}}"
- "tool interface versioning {{date}}"

_Source: [URL]_ for key findings.

---

## Verification Checklist

- [ ] Interface documented
- [ ] Tenant context specified
- [ ] Error cases covered
- [ ] Rate limits defined
- [ ] Side effects documented
- [ ] Tests written
- [ ] Version tracked

---

## Change Log

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0.0 | {{date}} | {{author}} | Initial tool contract |
