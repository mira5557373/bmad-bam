---
name: event-architecture
description: Document event-driven architecture with tenant isolation
category: architecture
version: 2.0.0
type: template
web_research_enabled: true
---

## Purpose

Document event-driven architecture design with tenant-aware event routing

# Event Architecture - {{project_name}}

> Generated by `bmad-bam-event-architecture` workflow.

**Project:** {{project_name}}
**Date:** {{date}}

---

## Event Catalog

<!-- FILL: Document all domain events -->
| Event Name | Domain | Payload Schema | Publishers | Subscribers |
|------------|--------|----------------|------------|-------------|
| tenant.created | Tenant | TenantCreatedEvent | TenantService | Billing, Notification |
| user.invited | IAM | UserInvitedEvent | UserService | Email, Audit |
| subscription.changed | Billing | SubscriptionChangedEvent | BillingService | Entitlements, Notification |

---

## Event Schema (CloudEvents)

```yaml
specversion: "1.0"
type: "com.{{project_name}}.{domain}.{event}"
source: "/tenants/{tenant_id}/modules/{module}"
subject: "{entity_type}/{entity_id}"
id: "{uuid}"
time: "{iso8601}"
datacontenttype: "application/json"
data:
  tenant_id: "{tenant_id}"
  # event-specific payload
```

---

## Topic Design

<!-- FILL: Document topic/queue structure -->
| Topic | Pattern | Partitioning | Retention |
|-------|---------|--------------|-----------|
| tenant-events | Fanout | By tenant_id | 7 days |
| billing-events | Queue | By tenant_id | 30 days |
| audit-events | Fanout | By tenant_id | 90 days |

---

## Tenant Isolation

**Event Routing:**
- [ ] All events include `tenant_id` in envelope
- [ ] Topic-per-tenant for enterprise isolation
- [ ] Shared topics with tenant filtering for standard tiers

**Subscription Filtering:**
| Tier | Isolation Model | Filter Strategy |
|------|-----------------|-----------------|
| FREE | Shared topic | Message attribute filter |
| PRO | Shared topic | Message attribute filter |
| ENTERPRISE | Dedicated topic | Topic-level isolation |

---

## Event Flow Patterns

<!-- FILL: Document event choreography -->
### Tenant Onboarding Flow

```
TenantCreated
    ├── → BillingService (create account)
    ├── → ProvisioningService (setup resources)
    └── → NotificationService (welcome email)
            └── TenantProvisioned
                    └── → AuditService (log completion)
```

---

## Dead Letter Handling

<!-- FILL: Document DLQ strategy -->
| DLQ | Source | Retry Policy | Alerting |
|-----|--------|--------------|----------|
| tenant-events-dlq | tenant-events | 3 retries, exponential | PagerDuty |
| billing-events-dlq | billing-events | 5 retries, linear | Slack |

---

## Ordering Guarantees

| Event Type | Ordering Requirement | Implementation |
|------------|---------------------|----------------|
| Tenant lifecycle | Strict per-tenant | Partition by tenant_id |
| User actions | Strict per-user | Partition by user_id |
| Analytics | None | Round-robin |

---

## Idempotency

**Producer:**
- [ ] Include idempotency key in event metadata
- [ ] Deduplication window: 24 hours

**Consumer:**
- [ ] Track processed event IDs
- [ ] Idempotent handlers for all state changes

---

## Web Research Queries

- "event-driven architecture multi-tenant {date}"
- "CloudEvents specification best practices {date}"
- "event sourcing tenant isolation {date}"

_Source: [URL]_ for key findings.

---

## Verification Checklist

- [ ] Event catalog complete
- [ ] CloudEvents schema compliant
- [ ] Tenant isolation verified
- [ ] DLQ strategy defined
- [ ] Ordering guarantees documented
- [ ] Idempotency implemented

---

## Change Log

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0.0 | {{date}} | {{author}} | Initial event architecture |
