---
name: mcp-server-config
description: MCP server configuration and multi-tenant isolation template
category: mcp
version: 1.0.0
type: template
web_research_enabled: true
source_verification: true
related_patterns: [mcp-server-lifecycle, mcp-tenant-isolation, mcp-tool-discovery, mcp-authentication]
---

## Purpose

Document MCP (Model Context Protocol) server configuration with tenant isolation and tool management

# MCP Server Configuration - {{project_name}}

> Generated by `bmad-bam-mcp-configuration` workflow.

**Project:** {{project_name}}
**Date:** {{date}}
**Version:** 1.0.0
**Author:** {{author}}

---

## MCP Server Architecture

<!-- FILL: Document MCP server deployment model -->
| Component | Purpose | Tenant Scope |
|-----------|---------|--------------|
| MCP Gateway | Request routing | Multi-tenant |
| Tool Registry | Tool discovery | Per-tenant |
| Permission Engine | Access control | Per-tenant |
| Result Cache | Response caching | Tenant-isolated |

### Deployment Model

```yaml
mcp_deployment:
  mode: enum[shared, dedicated, hybrid]
  scaling: enum[horizontal, vertical]
  isolation_level: enum[process, container, namespace]
```

---

## Tool Discovery Configuration

<!-- FILL: Document tool discovery settings -->
| Setting | Value | Description |
|---------|-------|-------------|
| Discovery Mode | [static/dynamic] | How tools are discovered |
| Registry URL | {{mcp_registry_url}} | Tool registry endpoint |
| Refresh Interval | 5 minutes | Tool metadata refresh |
| Schema Validation | strict | JSON Schema validation mode |

### Tool Categories

| Category | Tools | Tenant Access |
|----------|-------|---------------|
| Core | read, write, search | All tiers |
| Advanced | analyze, transform | Pro+ |
| Enterprise | custom, integrate | Enterprise |

---

## Tenant Isolation

<!-- FILL: Document tenant isolation for MCP -->
| Layer | Isolation Method | Verification |
|-------|------------------|--------------|
| Request | X-Tenant-ID header | Middleware |
| Tool Access | Permission matrix | Policy engine |
| Result Cache | Tenant-prefixed keys | Cache layer |
| Audit | Tenant-scoped logs | Logging |

### Permission Model

```yaml
mcp_permissions:
  tenant_id: uuid
  tools:
    - tool_id: string
      actions: [execute, configure]
      rate_limit: int
      
  tier_overrides:
    free:
      max_tools: 5
      rate_limit: 100/hour
    pro:
      max_tools: 20
      rate_limit: 1000/hour
    enterprise:
      max_tools: unlimited
      rate_limit: custom
```

---

## Authentication & Authorization

<!-- FILL: Document MCP auth configuration -->
| Method | Use Case | Implementation |
|--------|----------|----------------|
| API Key | Tool execution | MCP Gateway |
| OAuth 2.0 | User context | Identity Provider |
| Service Token | Server-to-server | mTLS |

### Token Configuration

| Token Type | Lifetime | Scope |
|------------|----------|-------|
| Access Token | 1 hour | tool:execute |
| Refresh Token | 24 hours | token:refresh |
| Service Token | 7 days | server:admin |

---

## Rate Limiting

<!-- FILL: Document rate limits per tier -->
| Tier | Requests/Min | Daily Limit | Burst |
|------|--------------|-------------|-------|
| Free | 10 | 1,000 | 20 |
| Pro | 100 | 50,000 | 200 |
| Enterprise | Custom | Custom | Custom |

### Throttling Strategy

| Condition | Action | Recovery |
|-----------|--------|----------|
| Rate exceeded | 429 response | Wait + retry |
| Quota exhausted | Block + notify | Next period |
| Abuse detected | Temporary ban | Manual review |

---

## Result Caching

<!-- FILL: Document caching strategy -->
| Cache Level | TTL | Invalidation |
|-------------|-----|--------------|
| Tool metadata | 5 min | On registry update |
| Schema | 1 hour | On version change |
| Results | Configurable | Request-specific |

### Cache Key Format

```
mcp:{{tenant_id}}:{tool_id}:{operation}:{hash(params)}
```

---

## Schema Validation

<!-- FILL: Document schema validation rules -->
| Validation | Level | Action on Failure |
|------------|-------|-------------------|
| Input schema | Strict | Reject request |
| Output schema | Warn | Log + allow |
| Tool schema | Strict | Disable tool |

---

## Monitoring & Observability

<!-- FILL: Document MCP metrics -->
| Metric | Type | Alert Threshold |
|--------|------|-----------------|
| tool_execution_latency | Histogram | p99 > 5s |
| tool_error_rate | Counter | > 5% |
| permission_denied | Counter | > 100/min |
| cache_hit_rate | Gauge | < 80% |

---

## Web Research Queries

- "MCP Model Context Protocol best practices {{date}}"
- "multi-tenant tool orchestration patterns {{date}}"
- "MCP server deployment architecture {{date}}"
- "tool permission management SaaS {{date}}"

_Source: [URL]_ for implementation guidance.

---

## Verification Checklist

- [ ] MCP server architecture documented
- [ ] Tool discovery configured
- [ ] Tenant isolation verified
- [ ] Authentication configured
- [ ] Rate limiting implemented
- [ ] Result caching configured
- [ ] Schema validation enabled
- [ ] Monitoring active
- [ ] **CRITICAL:** No cross-tenant tool access possible

---

## Change Log

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0.0 | {{date}} | {{author}} | Initial MCP server configuration |
