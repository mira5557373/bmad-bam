# BAM Security Extensions for bmad-agent-architect
# Adds secrets management, threat modeling, incident response, SSO/OAuth

[agent]
activation_steps_append = [
  "BAM Security capabilities available. Use ZSO for operations, ZAI for auth integration.",
]

persistent_facts = [
  "file:{project-root}/_bmad/bam/data/context/bam-core.md",
  "file:{project-root}/_bmad/bam/data/patterns/secrets-management.md",
  "file:{project-root}/_bmad/bam/data/patterns/incident-response.md",
  "file:{project-root}/_bmad/bam/data/patterns/zero-trust.md",
  "file:{project-root}/_bmad/bam/data/patterns/sso-auth.md",
]

principles = [
  "BAM Security: Zero trust for all tenant boundaries.",
  "BAM Security: Secrets must be tenant-isolated and rotatable.",
  "BAM Security: Incident response must include tenant notification.",
  "BAM Security: Authentication must respect tenant context in all flows.",
  "BAM Gates: QG-S3 (Security Baseline), QG-S5 (Auth), QG-IR (Incident Response).",
]

[[agent.menu]]
code = "ZSO"
description = "Security Operations: secrets, threat modeling, incident response"
skill = "bmad-bam-security-operations"

[[agent.menu]]
code = "ZPZT"
description = "Load: Zero Trust pattern details"
prompt = """
Loading zero trust pattern:
`{project-root}/_bmad/bam/data/patterns/zero-trust.md`

Confirm loaded. Ready for zero trust architecture guidance.
"""

[[agent.menu]]
code = "ZPSM"
description = "Load: Secrets Management pattern details"
prompt = """
Loading secrets management pattern:
`{project-root}/_bmad/bam/data/patterns/secrets-management.md`

Confirm loaded. Ready for secrets architecture guidance.
"""

[[agent.menu]]
code = "ZPIR"
description = "Load: Incident Response pattern details"
prompt = """
Loading incident response pattern:
`{project-root}/_bmad/bam/data/patterns/incident-response.md`

Confirm loaded. Ready for incident response guidance.
"""

[[agent.menu]]
code = "ZAI"
description = "Auth Integration: SSO, OAuth, API keys, session management"
skill = "bmad-bam-auth-integration"

[[agent.menu]]
code = "ZPSA"
description = "Load: SSO & Authentication pattern details"
prompt = """
Loading SSO & authentication pattern:
`{project-root}/_bmad/bam/data/patterns/sso-auth.md`

Confirm loaded. Ready for authentication architecture guidance.
"""

[[agent.menu]]
code = "ZPD"
description = "Load: Prompt Injection Detection pattern"
prompt = """
Loading prompt injection detection pattern:
`{project-root}/_bmad/bam/data/patterns/prompt-injection-detection.md`

Use web search for current implementation:
- "prompt injection detection patterns {date}"
- "LLM jailbreak prevention techniques {date}"

Confirm loaded. Ready for prompt injection defense guidance.
"""

[[agent.menu]]
code = "ZKS"
description = "Load: Kill Switch Registry pattern"
prompt = """
Loading kill switch registry pattern:
`{project-root}/_bmad/bam/data/patterns/kill-switch-registry.md`

Use web search for current implementation:
- "AI agent kill switch patterns {date}"
- "emergency shutdown multi-tenant systems {date}"

Confirm loaded. Ready for emergency shutdown design.
"""

# NEXUS Phase 2 Security Patterns

[[agent.menu]]
code = "ZSF"
description = "Load: Semantic Firewall pattern (content policy)"
prompt = """
Loading semantic firewall pattern:
`{project-root}/_bmad/bam/data/patterns/semantic-firewall.md`

Use web search for current implementation:
- "semantic firewall LLM production patterns {date}"
- "AI content policy engine implementation {date}"
- "embedding-based content moderation {date}"

Confirm loaded. Ready for semantic firewall design.
"""

[[agent.menu]]
code = "ZOS"
description = "Load: Output Sanitization pattern (PII/secrets)"
prompt = """
Loading output sanitization pattern:
`{project-root}/_bmad/bam/data/patterns/output-sanitization.md`

Use web search for current implementation:
- "LLM output PII detection redaction {date}"
- "AI secret scanning output filtering {date}"
- "GDPR compliant AI output {date}"

Confirm loaded. Ready for output sanitization design.
"""

[[agent.menu]]
code = "ZRT"
description = "Load: RBAC Per Tool pattern (agent permissions)"
prompt = """
Loading RBAC per tool pattern:
`{project-root}/_bmad/bam/data/patterns/rbac-per-tool.md`

Use web search for current implementation:
- "AI agent tool RBAC patterns {date}"
- "LangGraph tool permissions {date}"
- "MCP tool access control {date}"

Confirm loaded. Ready for tool permission design.
"""

# NEXUS Phase 3 Security Patterns

[[agent.menu]]
code = "ZSL"
description = "Load: Secret Leak Detector pattern (credential scanning)"
prompt = """
Loading secret leak detector pattern:
`{project-root}/_bmad/bam/data/patterns/secret-leak-detector.md`

Use web search for current implementation:
- "secret detection LLM prompts {date}"
- "API key scanning AI systems {date}"
- "TruffleHog GitLeaks AI integration {date}"

Confirm loaded. Ready for secret detection design.
"""

[[agent.menu]]
code = "ZCN"
description = "Load: Canary Token Inserter pattern (leak tracking)"
prompt = """
Loading canary token inserter pattern:
`{project-root}/_bmad/bam/data/patterns/canary-token-inserter.md`

Use web search for current implementation:
- "canary token prompt tracking {date}"
- "LLM data exfiltration detection {date}"
- "honeypot tokens AI security {date}"

Confirm loaded. Ready for canary token design.
"""

[[agent.menu]]
code = "ZTS"
description = "Load: Tool SBOM Registry pattern (supply chain security)"
prompt = """
Loading tool SBOM registry pattern:
`{project-root}/_bmad/bam/data/patterns/tool-sbom-registry.md`

Use web search for current implementation:
- "software bill of materials AI tools {date}"
- "MCP server security SBOM {date}"
- "CycloneDX SPDX AI integration {date}"

Confirm loaded. Ready for tool SBOM design.
"""

[[agent.menu]]
code = "ZSD"
description = "Load: Streaming Output Decoder pattern (real-time safety)"
prompt = """
Loading streaming output decoder pattern:
`{project-root}/_bmad/bam/data/patterns/streaming-output-decoder.md`

Use web search for current implementation:
- "streaming LLM output safety filtering {date}"
- "real-time content moderation streaming {date}"
- "SSE stream content filtering patterns {date}"

Confirm loaded. Ready for streaming safety design.
"""
