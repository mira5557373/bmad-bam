type: workflow
name: churn-prediction
displayName: Churn Prediction
description: "Design churn prediction model with leading indicators, risk scoring, and intervention strategies. Use when the user requests to 'design churn prediction' or 'plan retention analytics'."
module: bam
config_variables:
  - tenant_model
