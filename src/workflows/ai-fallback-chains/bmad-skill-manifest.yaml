type: workflow
name: ai-fallback-chains
displayName: AI Fallback Chains
description: "Design multi-provider fallback with quality thresholds. Use when the user requests to 'design AI fallbacks' or 'create provider failover'."
module: bam
