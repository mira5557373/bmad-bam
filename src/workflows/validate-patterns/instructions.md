# Instructions

This workflow validates BAM pattern registry files.

1. Load all pattern registry CSV files
2. Validate structure and required columns
3. Check dependencies and conflicts
4. Validate web query format
5. Cross-reference with step files
6. Generate validation report
