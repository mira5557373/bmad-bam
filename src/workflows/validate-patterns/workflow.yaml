# Workflow metadata for bmad-bam-validate-patterns
config_variables:
  - user_name
  - communication_language
  - document_output_language
  - output_folder
required_tools: []
tags: [bam, validation, patterns]
mode: complex
