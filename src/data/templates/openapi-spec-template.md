---
name: openapi-spec-template
description: OpenAPI specification document for multi-tenant platforms
category: integration
version: 1.0.0
type: template
web_research_enabled: true
source_verification: true
---

# OpenAPI Specification: {{api_name}}

> Generated by BAM module. OpenAPI specification for multi-tenant SaaS platforms.

## Document Metadata

| Field              | Value                   |
| ------------------ | ----------------------- |
| Document ID        | OAS-{{version}}         |
| API Name           | {{api_name}}            |
| OpenAPI Version    | 3.0.3                   |
| Date               | {{date}}                |
| Author             | {{author}}              |
| Status             | {{status}}              |

---

## Specification

```yaml
openapi: 3.0.3
info:
  title: {{api_title}}
  description: |
    {{api_description}}
    
    ## Multi-Tenant Support
    This API supports multi-tenant operations. Tenant context is determined by:
    - JWT `tenant_id` claim (preferred)
    - `X-Tenant-ID` header (fallback)
    
    ## Rate Limiting
    Rate limits vary by tier:
    - Free: 60 req/min
    - Pro: 300 req/min
    - Enterprise: Custom
    
  version: {{api_version}}
  contact:
    name: {{contact_name}}
    email: {{contact_email}}
    url: {{contact_url}}
  license:
    name: {{license_name}}
    url: {{license_url}}
  termsOfService: {{tos_url}}

servers:
  - url: https://api.{{domain}}/v{{major_version}}
    description: Production
  - url: https://staging-api.{{domain}}/v{{major_version}}
    description: Staging

tags:
  - name: {{tag_1}}
    description: {{tag_1_desc}}
  - name: {{tag_2}}
    description: {{tag_2_desc}}
  - name: {{tag_3}}
    description: {{tag_3_desc}}

paths:
  /{{resource}}:
    get:
      tags:
        - {{resource_tag}}
      summary: List {{resource}}
      description: Retrieve a paginated list of {{resource}} for the current tenant.
      operationId: list{{Resource}}
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/LimitParam'
        - $ref: '#/components/parameters/SortParam'
        - $ref: '#/components/parameters/FilterParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/{{Resource}}ListResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/RateLimited'
      security:
        - BearerAuth: []
      x-rate-limit:
        free: 60/min
        pro: 300/min
        enterprise: 1000/min
    
    post:
      tags:
        - {{resource_tag}}
      summary: Create {{resource}}
      description: Create a new {{resource}} for the current tenant.
      operationId: create{{Resource}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/{{Resource}}CreateRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/{{Resource}}'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationError'
      security:
        - BearerAuth: []

  /{{resource}}/{id}:
    get:
      tags:
        - {{resource_tag}}
      summary: Get {{resource}}
      description: Retrieve a specific {{resource}} by ID.
      operationId: get{{Resource}}
      parameters:
        - $ref: '#/components/parameters/IdParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/{{Resource}}'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
      security:
        - BearerAuth: []
    
    put:
      tags:
        - {{resource_tag}}
      summary: Update {{resource}}
      description: Update an existing {{resource}}.
      operationId: update{{Resource}}
      parameters:
        - $ref: '#/components/parameters/IdParam'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/{{Resource}}UpdateRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/{{Resource}}'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
      security:
        - BearerAuth: []
    
    delete:
      tags:
        - {{resource_tag}}
      summary: Delete {{resource}}
      description: Delete a {{resource}}. This action cannot be undone.
      operationId: delete{{Resource}}
      parameters:
        - $ref: '#/components/parameters/IdParam'
      responses:
        '204':
          description: Deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
      security:
        - BearerAuth: []

components:
  schemas:
    {{Resource}}:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        tenant_id:
          type: string
          format: uuid
          readOnly: true
        {{field_1}}:
          type: {{field_1_type}}
          description: {{field_1_desc}}
        {{field_2}}:
          type: {{field_2_type}}
          description: {{field_2_desc}}
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
        - {{field_1}}
    
    {{Resource}}CreateRequest:
      type: object
      properties:
        {{field_1}}:
          type: {{field_1_type}}
          description: {{field_1_desc}}
        {{field_2}}:
          type: {{field_2_type}}
          description: {{field_2_desc}}
      required:
        - {{field_1}}
    
    {{Resource}}UpdateRequest:
      type: object
      properties:
        {{field_1}}:
          type: {{field_1_type}}
          description: {{field_1_desc}}
        {{field_2}}:
          type: {{field_2_type}}
          description: {{field_2_desc}}
    
    {{Resource}}ListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/{{Resource}}'
        pagination:
          $ref: '#/components/schemas/Pagination'
    
    Pagination:
      type: object
      properties:
        page:
          type: integer
        limit:
          type: integer
        total:
          type: integer
        total_pages:
          type: integer
        has_next:
          type: boolean
        has_previous:
          type: boolean
    
    Error:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            details:
              type: array
              items:
                type: object
                properties:
                  field:
                    type: string
                  reason:
                    type: string
            request_id:
              type: string
            timestamp:
              type: string
              format: date-time
  
  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: Resource ID
    
    PageParam:
      name: page
      in: query
      schema:
        type: integer
        default: 1
        minimum: 1
      description: Page number
    
    LimitParam:
      name: limit
      in: query
      schema:
        type: integer
        default: 20
        minimum: 1
        maximum: 100
      description: Items per page
    
    SortParam:
      name: sort
      in: query
      schema:
        type: string
        default: created_at
      description: Sort field
    
    FilterParam:
      name: filter
      in: query
      schema:
        type: string
      description: Filter expression
  
  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    
    Forbidden:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    
    Conflict:
      description: Conflict
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    
    ValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    
    RateLimited:
      description: Rate limited
      headers:
        X-RateLimit-Limit:
          schema:
            type: integer
        X-RateLimit-Remaining:
          schema:
            type: integer
        X-RateLimit-Reset:
          schema:
            type: integer
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT authentication with tenant_id claim
    
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
      description: API key for service-to-service auth
```

---

## Web Research Queries

Before finalizing this document, verify current best practices:

- "OpenAPI 3.0 specification best practices {date}"
- "REST API design patterns {date}"
- "API schema design multi-tenant {date}"

Incorporate relevant findings. _Source: [URL]_

---

## Verification Checklist

- [ ] **CRITICAL:** OpenAPI 3.0.3+ compliant
- [ ] **CRITICAL:** All endpoints documented
- [ ] **CRITICAL:** Security schemes defined
- [ ] All schemas complete
- [ ] Error responses standardized
- [ ] Pagination documented
- [ ] Rate limits specified
- [ ] Multi-tenant context documented

---

## Change Log

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| {{version}} | {{date}} | {{author}} | Initial document |
