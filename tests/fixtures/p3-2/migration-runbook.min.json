{
  "schema_version": "1.0",
  "decided_at": "2026-05-17T14:23:00Z",
  "migration_axes": ["tier"],
  "zero_downtime_required": false,
  "per_axis": {
    "tier": {
      "cohort_plan": {
        "cohort_selection_method": "rollout_tier_hint_driven",
        "cohorts": [
          { "id": "c1", "tier_ids": ["lite"], "size_estimate": 10 }
        ]
      },
      "dry_run_plan": {
        "dry_run_environment": "staging",
        "cadence": "once_pre_rollout"
      },
      "rollback_gate": {
        "rollback_trigger": ["error_rate_above_baseline"],
        "rollback_method": "canary_revert"
      },
      "abort_criteria": [
        { "id": "abort_error_rate", "metric": "error_rate", "threshold": "5%", "window": "5m" }
      ],
      "observability_hooks": [
        { "id": "obs_p99_latency", "signal": "p99_latency_ms", "alert_threshold": "200ms" }
      ]
    }
  }
}
